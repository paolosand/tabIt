/** Stub for Task 4/5: renders the overlay UI into `shadowRoot` for the given `videoId`
 *  and returns a stop function to tear it down. This task only wires the plumbing. */
export function renderOverlay(_shadowRoot: ShadowRoot, _videoId: string): () => void {
  return () => {};
}
